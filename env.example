# Gemini API Key (Optional, for AI features)
GEMINI_API_KEY=

# Google Apps Script Web App URL
# 1. Deploy gas-backend.gs as a Web App
# 2. Paste the URL here to use GAS as the backend instead of the local SQLite database
VITE_GAS_URL=
